apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: pg-school-backup
  namespace: pg-school
spec:
  configuration:
    destinationPath: s3://pg-school-backup
    endpointURL: http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc.cluster.local
    s3Credentials:
      accessKeyId:
        key: ACCESS_KEY_ID
        name: school-s3-wals
      secretAccessKey:
        key: ACCESS_SECRET_KEY
        name: school-s3-wals
      region:
        key: region
        name: school-s3-wals
    wal:
      compression: gzip
      maxParallel: 4
  retentionPolicy: 10d
